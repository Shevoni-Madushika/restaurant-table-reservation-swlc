// Environment configuration
const config = {
  // Backend API Base URL (Spring Boot running separately)
  API_BASE_URL: 'http://localhost:8080',
  
  // App configuration
  APP_NAME: 'TableTop.lk',
  APP_VERSION: '1.0.0',
  
  // Frontend configuration
  FRONTEND_PORT: 3000,
  BACKEND_PORT: 8080,
};

export default config;
